plugins:
  FileTransport:
    classpath:
      - ${APAMA_WORK}/lib/file-transport-plugins.jar
    class: com.apama.samples.FileTransport
    
  RegexCodec:
    classpath: ${APAMA_WORK}/lib/regex-codec-sample.jar
    class: com.apama.samples.RegexCodec


startChains:
  testChain:
    - apama.eventMap:

    - RegexCodec:
       regex: "pattern"
       repl: "repl"
       keys:
         - payload
       invalid-config-option: foo bar
        
    - FileTransport:
        hostToFile: output.txt
        fileToHost: input.txt
